global:
  scrape_interval: 15s # Seberapa sering scrape metrics
  evaluation_interval: 15s

scrape_configs:
  - job_name: "golang-app"
    static_configs:
      - targets: ["host.docker.internal:8080"]
        labels:
          app: "ai-cv-evaluator-monitoring"
          env: "dev"
