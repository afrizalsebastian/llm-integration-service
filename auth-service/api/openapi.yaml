openapi: 3.1.0
info:
  title: Auth Service
  version: 1.0.0
servers:
  - url: /api/v1

paths:
  /auth/google:
    get:
      summary: reserve google auth 2.0
  /auth/google/callback:
    get:
      summary: callback url after user google login
